# Arquitectura del Sistema SGE ASI

## 📐 Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────┐
│                    USUARIO FINAL                             │
│                  (Navegador Web)                             │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ HTTP/HTTPS
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                  FRONTEND (React + Vite)                     │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐     │
│  │  Páginas    │  │  Componentes │  │   Servicios    │     │
│  │  - Auth     │  │  - Common    │  │  - API Client  │     │
│  │  - Dashboard│  │  - Layout    │  │  - Auth        │     │
│  │  - Módulos  │  │  - Modules   │  │  - Modules     │     │
│  └─────────────┘  └──────────────┘  └────────────────┘     │
│                                                              │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────┐     │
│  │  Context    │  │   Layouts    │  │     Hooks      │     │
│  │  - Auth     │  │  - Main      │  │  - Custom      │     │
│  │  - Theme    │  │  - Auth      │  │  - API         │     │
│  └─────────────┘  └──────────────┘  └────────────────┘     │
└────────────────────┬────────────────────────────────────────┘
                     │
                     │ REST API (JSON)
                     │ JWT Authentication
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                 API (Node.js + Express)                      │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              MIDDLEWARE LAYER                        │   │
│  │  - JWT Auth    - CORS    - Helmet                    │   │
│  │  - Tenant      - Error   - Validation                │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │               ROUTES LAYER                           │   │
│  │  /auth  /tenants  /cost-centers  /inventory          │   │
│  │  /documents  /clients  /employees  /portfolio        │   │
│  │  /treasury  /suppliers                               │   │
│  └──────────────┬───────────────────────────────────────┘   │
│                 │                                            │
│  ┌──────────────▼───────────────────────────────────────┐   │
│  │            CONTROLLERS LAYER                         │   │
│  │  - Request validation                                │   │
│  │  - Response formatting                               │   │
│  │  - Error handling                                    │   │
│  └──────────────┬───────────────────────────────────────┘   │
│                 │                                            │
│  ┌──────────────▼───────────────────────────────────────┐   │
│  │             SERVICES LAYER                           │   │
│  │  - Business logic                                    │   │
│  │  - Data validation                                   │   │
│  │  - Tenant isolation                                  │   │
│  └──────────────┬───────────────────────────────────────┘   │
│                 │                                            │
│  ┌──────────────▼───────────────────────────────────────┐   │
│  │              MODELS LAYER                            │   │
│  │  - Sequelize ORM                                     │   │
│  │  - Data structure                                    │   │
│  │  - Relationships                                     │   │
│  └──────────────┬───────────────────────────────────────┘   │
└─────────────────┼───────────────────────────────────────────┘
                  │
                  │ SQL Queries
                  ▼
┌─────────────────────────────────────────────────────────────┐
│                    MYSQL DATABASE                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Users      │  │   Tenants    │  │ Cost Centers │      │
│  │   Roles      │  │   Config     │  │  Categories  │      │
│  │   Audit      │  │   Branding   │  │    Items     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Inventory   │  │  Documents   │  │   Clients    │      │
│  │  Movements   │  │  Versions    │  │  Contracts   │      │
│  │   Alerts     │  │  Metadata    │  │  Projects    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Employees   │  │  Portfolio   │  │  Treasury    │      │
│  │  Timesheets  │  │  Invoices    │  │  Payments    │      │
│  │  Training    │  │  Payments    │  │  Cash Flow   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│  ┌──────────────┐                                           │
│  │  Suppliers   │  [Multitenant: tenantId en todas]        │
│  │  PO Orders   │                                           │
│  │  Receptions  │                                           │
│  └──────────────┘                                           │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Flujo de Datos

### Autenticación

```
Usuario → Login Page → API /auth/login
                           ↓
                    Validate Credentials
                           ↓
                     Generate JWT Token
                           ↓
                    Return Token + User
                           ↓
                  Store in localStorage
                           ↓
                  Add to API Headers
                           ↓
              All subsequent requests
```

### Operación CRUD Típica

```
User Action → Component
                 ↓
            Service Call (Axios)
                 ↓
            API Route (+ JWT)
                 ↓
            Middleware (Auth, Tenant)
                 ↓
            Controller (Validate)
                 ↓
            Service (Business Logic)
                 ↓
            Model (Database)
                 ↓
            MySQL Query
                 ↓
            Return Data
                 ↓
            Format Response
                 ↓
            Update UI
```

## 🏗️ Estructura de Carpetas

```
SGE_ASI/
│
├── FRONT/                          # Frontend Application
│   ├── src/
│   │   ├── components/
│   │   │   ├── common/            # Reutilizables (Button, Input, Card)
│   │   │   ├── layout/            # Header, Sidebar, Footer
│   │   │   └── modules/           # Específicos por módulo
│   │   ├── pages/                 # Vistas principales
│   │   │   ├── auth/              # Login, Register
│   │   │   ├── dashboard/         # Dashboard
│   │   │   └── [modules]/         # Páginas de módulos
│   │   ├── layouts/               # MainLayout, AuthLayout
│   │   ├── services/              # API clients
│   │   ├── context/               # Global state (Context API)
│   │   ├── hooks/                 # Custom hooks
│   │   └── utils/                 # Utilidades
│   ├── public/                    # Assets estáticos
│   ├── Dockerfile                 # Container config
│   └── package.json               # Dependencies
│
├── API/                            # Backend Application
│   ├── src/
│   │   ├── config/                # Database, Swagger
│   │   ├── middleware/            # Auth, Tenant, Errors
│   │   ├── models/                # Sequelize models
│   │   ├── routes/                # Express routes
│   │   ├── controllers/           # Request handlers
│   │   ├── services/              # Business logic
│   │   └── utils/                 # Helpers
│   ├── tests/
│   │   ├── unit/                  # Unit tests
│   │   └── integration/           # Integration tests
│   ├── database/
│   │   └── init.sql               # DB initialization
│   ├── Dockerfile                 # Container config
│   └── package.json               # Dependencies
│
├── docs/                           # Documentation (if needed)
├── docker-compose.yml              # Orchestration
├── .env.example                    # Environment template
├── .gitignore                      # Git ignore rules
├── README.md                       # Main documentation
├── QUICKSTART.md                   # Quick start guide
├── CONTRIBUTING.md                 # Contribution guide
├── PROJECT_STATUS.md               # Current status
└── tareas_copilot.md              # Development tasks
```

## 🔐 Seguridad

### Autenticación JWT

```
┌──────────────┐
│   Usuario    │
└──────┬───────┘
       │
       │ 1. Login (email/password)
       ▼
┌──────────────┐
│  API /login  │
└──────┬───────┘
       │
       │ 2. Validate & Hash
       ▼
┌──────────────┐
│   Database   │
└──────┬───────┘
       │
       │ 3. Generate JWT
       ▼
┌──────────────┐
│  JWT Token   │◄────┐
└──────┬───────┘     │
       │              │
       │ 4. Return    │ 6. Include in
       ▼              │    all requests
┌──────────────┐     │
│  Frontend    │─────┘
└──────────────┘
```

### Multitenant

```
Request → Extract Tenant ID (Header/JWT)
            ↓
        Validate Tenant
            ↓
        Filter Data by tenantId
            ↓
        Return Only Tenant Data
```

## 🗄️ Modelo de Datos Multitenant

```sql
-- Todos los modelos incluyen tenantId

┌─────────────┐     ┌─────────────┐
│   Tenant    │     │    User     │
├─────────────┤     ├─────────────┤
│ id (PK)     │◄────┤ tenantId    │
│ name        │     │ email       │
│ subdomain   │     │ password    │
│ logo        │     │ role        │
└─────────────┘     └─────────────┘
                           │
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ CostCenter  │    │  Inventory  │    │   Client    │
├─────────────┤    ├─────────────┤    ├─────────────┤
│ id (PK)     │    │ id (PK)     │    │ id (PK)     │
│ tenantId    │    │ tenantId    │    │ tenantId    │
│ ide         │    │ sku         │    │ name        │
│ client      │    │ name        │    │ nit         │
└─────────────┘    └─────────────┘    └─────────────┘
       │
       │ 1:N
       ▼
┌─────────────┐
│  Category   │
├─────────────┤
│ id (PK)     │
│ costCenterI │
│ name        │
│ type        │
└─────────────┘
       │
       │ 1:N
       ▼
┌─────────────┐
│    Item     │
├─────────────┤
│ id (PK)     │
│ categoryId  │
│ name        │
│ value       │
└─────────────┘
```

## 🚀 Despliegue

### Docker Compose

```yaml
services:
  mysql      ─┐
  api        ─┼─► Network: sge-network
  frontend   ─┤
  phpmyadmin ─┘

Volumes:
  - mysql_data (persistent)
  - api_uploads (persistent)
```

### Flujo de Despliegue

```
1. Build Images
   ├── Frontend (nginx + React build)
   └── API (Node.js)

2. Start Services
   ├── MySQL (wait for healthy)
   ├── API (depends on MySQL)
   └── Frontend (depends on API)

3. Initialize
   ├── Run init.sql
   ├── Sync Sequelize models
   └── Start accepting requests
```

## 📊 Stack Tecnológico

### Frontend
```
React 19
  ├── Vite (Build tool)
  ├── React Router (Routing)
  ├── TailwindCSS (Styling)
  ├── Axios (HTTP client)
  └── Context API (State management)
```

### Backend
```
Node.js 18
  ├── Express 5 (Web framework)
  ├── Sequelize 6 (ORM)
  ├── MySQL 8 (Database)
  ├── JWT (Authentication)
  ├── Bcrypt (Password hashing)
  └── Swagger (API docs)
```

### DevOps
```
Docker
  ├── docker-compose (Orchestration)
  ├── MySQL container
  ├── Node.js container
  └── Nginx container
```

---

**Versión**: 1.0.0  
**Última actualización**: 2026-01-07
